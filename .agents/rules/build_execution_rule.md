# Build Execution Rule

- **Do NOT trigger or execute GitHub Actions builds automatically (`gh workflow run`)** unless the user explicitly commands to build or triggers a build request.
- Always ask or wait for explicit user confirmation before triggering any remote GitHub Actions workflow builds.
